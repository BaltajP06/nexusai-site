{
  "name": "nexusai-proxy",
  "version": "1.0.0",
  "description": "Secure server-side proxy for NexusAI — keeps API keys off the browser",
  "main": "index.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0"
  }
}
