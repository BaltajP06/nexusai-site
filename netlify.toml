[build]
  publish = "."

# Proxy frontend API calls to the deployed backend service.
# Replace the placeholder below with your real Render or backend URL.
[[redirects]]
  from = "/api/*"
  to = "https://nexusai-site.onrender.com/api/:splat"
  status = 200
  force = true